# Web app — notes for Claude

- Vite + Vercel Function in `api/`. Vercel project Root Directory is `apps/web`.
- Check changes with `yarn build` and `yarn test --run` (run in this folder).
- The iOS app in `../mobile` mirrors `src/utils/{resolveExchange,currencyMeta,formatCurrency}.ts` — keep behaviour in sync.
